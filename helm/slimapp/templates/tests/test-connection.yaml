apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "slimapp.fullname" . }}-test-connection"
  labels:
    {{- include "slimapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "slimapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
